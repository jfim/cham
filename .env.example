# Required
SECRET_KEY_BASE=generate_with_mix_phx_gen_secret
POSTGRES_PASSWORD=changeme

# PostgreSQL connection (matches docker-compose postgres service)
POSTGRES_USER=cham
POSTGRES_DB=cham
DATABASE_URL=ecto://cham:changeme@postgres/cham

# Phoenix
PHX_HOST=localhost
PORT=4000

# API authentication
API_KEY=changeme

# Paths (defaults work with docker-compose volumes)
# ARCHIVE_PATH=./archive
# CONFIG_TOML_PATH=./config/cham.toml
